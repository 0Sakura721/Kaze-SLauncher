# Memory — Kaze-SLauncher（新版，工作区目录：NEW AGE KAZE-SLAUNCHER）

> 跨会话记忆文件。规则：动手前先读；有值得记住的信息就更新；过期条目删除或标注。
> 最近更新：2026-08-20（UI 全面走查 + 三处优化；MuMu x86_64 验证）

## 项目约定
- 包名 `com.kaze.newage`，应用名 "Kaze SLauncher"（Kaze 与 SLauncher 之间带空格；连字符仅为 GitHub 仓库名所需，用户 2026-08-14 明确），GPL-3.0（与旧版 v2/v3 可共存）。
- 2026-08-14：用户要求去除 New Age 标识与连字符（应用名/界面文案改回 "Kaze SLauncher"）；内部类名（NewAgeApp/NewAgeTheme）与包名 com.kaze.newage 保留（非用户可见）。
- 后端 100% 继承 Kaze-SLauncher v3（proot + Ubuntu rootfs + Java/eula/服务端/下载/控制台，已验证可构建）；前端为全新双主题设计系统。
- 两主题：`clear`（简洁面板，默认）/ `glass`（液态玻璃）；原 aurora 极光已按用户要求替换，`fromId("aurora")` 自动迁移到 glass（2026-08-14）。存 SharedPreferences `theme_mode`；深浅色 `force_dark`（null=跟随系统，**两主题均生效**——用户反馈过"玻璃强制浅色"，已修：GlassDark 配色 + GlassBackdrop 明暗双版 + BackgroundCard 顶部镜面高光条）。玻璃光斑参数：深色 alphas 0.16-0.38 半径 0.38-0.72m（留深底）；浅色底色加深 #BFD5F1 系 + 白光斑 0.8+。
- 签名元素：`StatusOrb`（状态球，双主题差异化渲染）。动效尊重系统「动画时长缩放=0」。

## 决策记录
- 2026-08-14：用户拍板：① 自包含 proot 沿用 v3（不装 Termux）；② MVP 精简首发=Vanilla+Paper+自定义导入（UI 隐藏其他核心类型，后端 CoreSources 仍支持全部）；③ 三主题，默认简洁面板（参考 PiliPlus 风格——B 站第三方客户端，即清爽现代卡片面板）+ Aurora + Terminal Neon。之后用户要求移除霓虹 → 最终两主题（clear + aurora）。
- 2026-08-14：包名选 `com.kaze.newage`（与旧版共存，未让用户逐项确认，属"最优方案直接做"）。

## 构建要点（重要）
- 构建必须设 `GRADLE_USER_HOME=D:\Deepseek Harness\Kaze-SLauncher\v3\.gradle-user-home`（复用 v3 的 1.4GB 缓存：Gradle 8.11.1 dist + AGP/Kotlin/Compose 依赖，版本与 v3 完全一致；删 v3 前需先迁移该缓存）+ `TMP/TEMP=工作区\.tmp`。
- 签名：`debug.keystore`（android/androiddebugkey），根目录。
- JDK：系统 PATH 的 Microsoft OpenJDK 21（AGP 8.7.3 兼容）。
- 命令：`cmd /c "set GRADLE_USER_HOME=... && set TMP=... && gradlew.bat assembleDebug --no-daemon"`；基线构建 3m05s 通过。
- `local.properties` 已写 `sdk.dir=C:/Users/Flash/AppData/Local/Android/Sdk`。

## 真机测试（vivo V2352GA，Android 16/API 36，Adreno 735，1260x2800，2026-08-15）
- 无线调试：`adb pair <IP:配对端口> <6位码>`（配对码刷新弹窗就变，失败后要新码）→ `adb connect <IP:连接端口>`（配对端口≠连接端口，连接端口看无线调试主页「IP 地址和端口」）。配对过一次后可直接 connect（密钥已存 PC）。mDNS 发现在本网络不可用。
- **vivo 屏蔽应用 Log.d**（logcat 看不到 app 自产日志 KazeGlass/KazeEnv，系统 GC 日志可见）——真机排查别依赖日志，用截图+像素采样。
- vivo 安装需手机弹窗确认（否则 INSTALL_FAILED_ABORTED: User rejected permissions）。
- **RuntimeShader 兼容性实测（vivo Adreno 735/API36）**：波浪+色差 shader（BiliPai 旧 FullBar 版）黑屏（含无三角多项式版）；SDF 边缘 shader（BiliPai 旧 LiquidGlassShader）✅；**Kyant0 圆角矩形折射透镜（BiliPai 现役 lens 同款）✅ 渲染正常**。规则：无三角函数 + 单次 img.eval 的 SDF 类 shader 可用；sin/cos 或多采样版本黑。模拟器 SwiftShader 一律跳过（probeGpuRenderer 黑名单）。
- **当前基线 =「现在的 BiliPai」版式（Miuix IosLiquidGlassNavigationBar，Apache-2.0 已记 NOTICES）**：底栏 = 24dp 边距浮动圆胶囊（64dp）+ 10dp 投影 + 三层结构（层①背景玻璃链=haze+饱和1.5+Kyant0透镜24/24dp，层②surfaceContainer a0.4，层③图标标签最上——**透镜只作用于背景层，绝不能套在内容上**（会把图标吞掉，"选项都没了"bug 根因））。背景=纯色渐变+柔光斑（用户要求不改背景）。卡片=Haze+饱和1.5。用户方向（2026-08-15）：**"先完善UI，不要塑料UI"**——拒绝扁平塑料感，走真玻璃（透镜+景深+高光）。
- **去"劣质大框架"（2026-08-15，用户反馈三页廉价大卡框）**：① BackgroundCard 玻璃模式默认**无发丝边框**（effectiveBorder=null；M3 保留 hairline；显式 border 参数仍生效）；② CardTitleLayout 玻璃分隔线软化为 white 0.10/0.30；③ ServerScreen **删除整页大卡**——工具条+列表直接铺背景，实例卡悬浮；④ HomeScreen **删除大面板卡**——状态球/信息行/环境行直接铺背景，选择器改为 itemColor 小型玻璃 chip（Surface onClick，圆角 16）。真机实测：三页无框架、选择器下拉正常、胶囊底栏导航正常。
- **真机 blur 实测在工作**（开/关像素有差异）。玻璃观感全部来自面板层。**注意：测试中改动的用户 prefs 要恢复原状（theme_mode_value 曾误改 1→已恢复 0 跟随系统）**。
- **真机 rootfs 部署失败的根因 = 内部存储 /data/user/0 写入不稳定（2026-08-15 实锤）**：症状——提取器完整跑完（dash 检查通过、apt 日志存在、"环境初始化完成"），但 usr/bin 在 setup 后变空、var/lib/apt/lists 空、rootfs 只有 11MB；proot-home/instances.json/prefs 等小写入正常。与模拟器存储损坏同病。**解法：设置 → 存放到外部存储（env_external=true）→ 重新部署**。外部存储实测完整成功：rootfs 134MB、usr/bin 287 文件、dash 存在、"Linux 环境就绪"、进入 Java 21 安装阶段。**adb 调试注意：`run-as pkg sh -c '相对路径'` 的 CWD 是 / 不是应用目录（写入会落到根目录报 ENOENT），一律用绝对路径 /data/user/0/<pkg>/...；`adb shell 2>/dev/null` 在 PowerShell 会当成本地路径报错，用 2>&1。**
- **设置页去框架（2026-08-15，用户"设置页还是有塑料似的边框，弄成和其它页面一样"）**：删除全部 BackgroundCard+CardTitleLayout 包装——外观/环境/通用三个分区改为纯文字标题（titleMedium）+ 内容直接铺背景；环境卡的「部署」按钮移到标题行右侧；通用手风琴保持（行+分隔线）。真机实测无卡片框架。cardBorderColor 仅剩小元素（chip/磁贴）使用。
- **gradle 构建沙箱注意**：DSH 沙箱为 workspace-write 时，Gradle 写工作区外的 GRADLE_USER_HOME（v3\.gradle-user-home）会被拒（wrapper .lck 拒绝访问）；需 danger-full-access 或确认策略。残留 .lck 可删。
- **ServerGuardService 前台守护（2026-08-16 重做成功）**：旧 GuardService 崩因为 startForegroundService 后 5 秒内没 startForeground + 类型权限缺失。正确写法：manifest 声明 `foregroundServiceType="specialUse"` + `FOREGROUND_SERVICE_SPECIAL_USE` 权限 + `PROPERTY_SPECIAL_USE_FGS_SUBTYPE` 属性（缺一 SecurityException）；onCreate 第一时间 `ServiceCompat.startForeground(this, id, notif, FOREGROUND_SERVICE_TYPE_SPECIAL_USE)`；渠道 IMPORTANCE_LOW；服务端启动→start、全部停止→stop（DefaultServerManager 持 appContext）。MainActivity 请求 POST_NOTIFICATIONS（拒绝仅隐藏通知）。真机实测 isForeground=true、退后台 90 秒应用+服务端进程均存活、无崩溃。**建议用户把应用加入电池优化白名单**。

## 真机环境部署通关记录（2026-08-16）
- **根因实锤：targetSdk≥29 应用的 W^X + zygote seccomp**（vivo Android 16/API36 实测）：① W^X 禁止 exec /data/data 下 app_data_file ELF（proot exec guest /bin/sh 报 EACCES）；② zygote seccomp 拦截 18+ 系统调用（chdir/chmod/getcwd/clone3…）。`run-as` 能跑通是因为它没有 seccomp 过滤器（/proc/<pid>/status Seccomp: 2 vs 0）。与 oonid/pr 文档《important-notes.md》三拦路虎描述 100% 吻合（他们已用 "W^X + seccomp + targetSdk 35 proot Android" 验证）。
- **最终方案（前人经验，oonid/pr，GPL-2.0-or-later 与 GPL-3.0 兼容）**：bundle 其修补版 proot 二进制到 `app/src/main/jniLibs/arm64-v8a/{libproot.so(2.5MB), libproot-loader.so(18KB)}`（jsdelivr 从 github.com/oonid/pr 下载，勿改动）。proot 直接从 **nativeLibraryDir** 运行（唯一允许 untrusted_app execve 的位置），PROOT_LOADER 指同目录 loader（mmap 加载 guest ELF 绕过 W^X），修补版内置 SIGSYS 处理器模拟被 seccomp 拦的系统调用。**启动参数照搬其 shared.rs**：`--rootfs --cwd=/ --change-id=0:0 --kill-on-exit --link2symlink --kernel-release=6.17.0-pr -b /dev -b /proc -b /sys -b /proc/self/fd:/dev/fd -b /dev/urandom:/dev/random -b rootfs/sys/.empty:/sys/fs/selinux -b cacheDir:/tmp -b rootfs/tmp:/dev/shm -b <实例目录>:/mnt`。环境：PROOT_TMP_DIR/TMPDIR=**内部 cacheDir**、PROOT_L2S_DIR=rootfs/.l2s（须与 rootfs 同文件系统，dpkg/apt 硬链接需要）、HOME/LANG/TERM。已删除 proot-home 资产提取（fixProotSonameLinks 等旧代码）。
- 备选快速方案（未用）：targetSdk 28 无 W^X/seccomp（oonid/pr 实测表 targetSdk 28 proot 直跑）。
- **extractNativeLibs 必须 true**（2026-08-16）：AGP 默认 run-from-apk 模式不落盘 .so，nativeLibraryDir 为空 → "proot 运行时缺失"。已在 manifest application 加 `android:extractNativeLibs="true"`。验证：装完 `ls /data/app/~~*/<pkg>-*/lib/arm64` 应看到 libproot.so。
- **MC 新版（26.x，如 26.1.1）需要 Java 25**（实测 bundler class file 69.0，Java 21 报 UnsupportedClassVersionError）。已改：JavaVersionInference（major≥26→25）、installedJdkVersions 加 25、设置页/新建实例 Java 列表加 25、InstanceStore.load 迁移旧档 javaMajor<25 自动升 25。Adoptium 直连下载 Temurin 25（api.adoptium.net，手机网络不受限）。
- **内部 rootfs 部署成功**：系统 tar 解压 + 全局 sync + dashReadable 实读校验重试 → 内部 rootfs 463MB 完整；JDK 348MB 外部→内部迁移成功（java-21-openjdk-arm64 完整可执行）。
- **真机 run-as 诊断法**：push 诊断脚本到 /data/local/tmp + `run-as com.kaze.newage sh /data/local/tmp/diag.sh`（CWD=/，脚本内绝对路径 + cd 目录 + 设 PROOT_LOADER/LD_LIBRARY_PATH/PROOT_NO_SECCOMP）。注意 run-as 无法访问 /storage/emulated/0 且无 seccomp，只能证明"文件与 proot 本身 OK"，应用内问题必须以应用自身日志定位（proot -v 4 输出会经 console 流落盘）。
- 资产长度校验：`assets.openFd` 对 compressed 资产抛异常 → 用 `assets.open().use { it.available().toLong() }`。
- 日志证据：`/storage/emulated/0/Android/data/com.kaze.newage/files/instances/man/console-output.log` 会累积历史会话错误，诊断前先 rm 再触发。
- **✅ 端到端跑通（2026-08-16 14:24，vivo 真机）**：应用内 启动服务端 → proot(nativeLibraryDir) → Ubuntu rootfs(内部463MB) → Temurin Java 25 → vanilla-26.1.1 → "Preparing level world / All chunks are saved / Server empty for 60 seconds, pausing"，adb forward 25565 TcpTestSucceeded=True。已提交推送 0815（62cb90e）。遗留：authlib 周期性网络 WARNING（无害）；局域网直连手机 IP 需手机防火墙放行（vivo 可能默认拦）。
- **2026-08-16 下午新增（已推送 0815，a6ea68b）**：①控制台右上角「保存日志」按钮（SAF 选目录一次性导出完整日志，默认名 `<实例>-<时间戳>.log`；用户明确不要实时写入，MediaStore 实时方案已移除——踩坑：MediaStore Downloads 的 IS_PENDING 流程必须先 open 写、后清 pending，否则行被删 open 报 No item；vivo 会把 console.log 改名 console.log.txt）；②电池白名单：启动服务端首次自动弹 ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS + 设置→守护与电池 手风琴（状态/重试/vivo 后台耗电管理引导）；③实例详情 server.properties 编辑器「空服自动暂停」chips（pause-when-empty-seconds 关闭/30s/60s/2m/5m，MC 1.21.2+）；④ServerGuardService 前台守护（specialUse，详见上）。

## 模拟器测试（MuMu 12，重要发现）
- adb 设备：emulator-5554（伪装 vivo V2203A），Android 15，主 ABI x86_64 但 **abilist 含 arm64-v8a**（houdini ARM 翻译层）→ `Build.SUPPORTED_ABIS` 判定走 arm64 路线，**proot aarch64 二进制可运行**（日志 `houdini: executing proot`）。
- 已实测通过：安装/启动、双主题切换+持久化、强制浅色（LocalDarkTheme bug 已修）、版本列表拉取、Paper 26.2 下载建实例（52MB jar）、server.properties 自动生成（端口 25565）。
- **环境部署完整通过两次**（18:59 旧解压器、19:29 新解压器）：proot 在 houdini 下运行、106MB rootfs 完整解压、**apt-get update 在 proot 内成功**（证明 tar/符号链接/proot/网络全链路 OK）。
- **模拟器存储损坏（环境问题，非应用 bug）——最终诊断**：重启 MuMu 虚拟机也无效（22:00 后验证）。特征：应用视角内「写后立读」都不保证——dash 提取完成检查通过（exists()=true）后毫秒级消失（isReady 复查=false，KazeEnv 日志实锤）；shell FUSE 视图显示 0 文件/18M 恒定。rootfs 解压 4 次完整成功（18:59/19:29/两轮外部存储）+ proot 内 apt 通过，之后始终卡住。**需重建 MuMu 模拟器实例或换真机**。大文件流式写入（jar/备份 zip）与 /data/local/tmp 正常。
- **主题体系（用户要求照搬 BiliPai，GPL-3.0 已记入 NOTICES）**：设置页「外观」含 BiliPai 全套——主题样式（M3/安卓原生液态玻璃）、主题模式（跟随系统/浅色/深色 `theme_mode_value`）、深色样式（普通黑/AMOLED纯黑 `dark_style`）、MD3 颜色来源（跟随壁纸/自定义 `md3_color_source`+`md3_custom_color` hex）、取色风格（materialkolor PaletteStyle 九种）、液态玻璃模式（清晰/均衡/磨砂 `glass_mode`，BiliPai progress lerp 参数）、原生模糊开关（`glass_blur`）。依赖：materialkolor **2.1.1**（4.x 是 Kotlin 2.3 编译不兼容；2.x API = `dynamicColorScheme(seedColor: Color, isDark, isAmoled, style=...)`）、Haze 1.6.10（1.7.x 需 compileSdk36）。**黑屏真凶是折射 RuntimeShader（API33+ SwiftShader 不支持），不是 Haze**——底栏已改用 Haze，真模糊模拟器实测可用（glass_blur ON 渲染正常）；LiquidGlassEffect.kt 折射着色器保留文件暂未接线（备真机启用）。
- **新增功能（Round 2）**：设置→环境卡「存放到外部存储」开关（`env_external`；`ProotEnvironment` 构造参数 `linuxBase: () -> File`，AppContainer 传入 provider；内部空间不足场景，切换后需重新部署）。
- TarExtractor **不要加 fd.sync()**：FUSE 外部存储上 sync 可能长时间阻塞（实测卡死）；已移除（Downloader 的 sync 保留，实测正常）。
- 修复过的问题：tar 软链条目 linkname 在 header offset 157（已修）；修复函数调用时机（fixProotSonameLinks 原在 rootfs 解压前调用）；`usr/*` 在注释里触发 Kotlin 嵌套块注释（编译错）；isReady 曾依赖 canExecute（模拟器不可靠，已移除并加诊断日志 KazeEnv）；TarExtractor/Downloader 已加 fd.sync()。
- **adb 截图必须 `cmd /c "adb exec-out screencap -p > file.png"`**（PowerShell `>` 重定向损坏二进制）；像素采样用 System.Drawing 验证配色。
- 测试教训：自动化坐标点击可能误触设置页「背景图」流程（保存暗图 + 遮罩）→ 全屏灰蒙层掩盖主题渲染；排查时先看 prefs 的 `bg_enabled` 与 `files/background.png`。pm clear 可重置。
- 背景图 UX 修复（2026-08-14，用户反馈"设置不了"）：①「选择背景图」按钮**常驻**（不再被开关隐藏；选图后自动开启显示）；②遮罩默认 60→**25**、范围 0~90（0=不遮，60% 黑遮罩曾让图片几乎不可见、像设置失败）；③滑杆仅在已选图时显示。开关实测功能正常（tap x≈830 标题行右侧）。
- **液态玻璃"没效果"根因修复（2026-08-14，用户三次反馈）**：①浅色模式光斑用 `BlendMode.Screen` 叠加在近白底上=数学上不可见（Screen 只适用于深底）→ 浅色改 SourceOver 直叠高饱和色斑；②背景全平滑渐变、无高频细节，模糊无从体现 → 新加 **bokeh 锐利光点**（drawCircle 小圆点，卡片模糊后与背景形成清晰度差）；③参数强化：blur 3→30 提到 **10→38dp**、surfaceAlpha 降到 0.10→0.34（更透）、Haze 加蓝色 Plus tint 0.06 + noiseFactor 0.05；④深色 bloom alpha 0.16-0.38 提到 0.30-0.60 + 新增粉/青斑；⑤BackgroundCard 深色镜面高光 0.35→0.55。模拟器实测：浅色边缘底 R138G168B239（饱和蓝，原灰白 206,208,212）卡内 R162G175B240 磨砂白蓝；深色深空蓝底+霓虹光斑。Haze 1.6.10 HazeStyle API：`HazeStyle(blurRadius, noiseFactor, fallbackTint, tints=listOf(HazeTint(color, BlendMode)))`（tint 单参是 fallbackTint）。
- **旧键迁移注意**：prefs 里残留 `force_dark`（旧语义 null=跟随系统）→ SettingsPrefs 迁移为 themeModeValue（false→1 强制浅色），会导致 `cmd uimode night` 不生效。测试深色需在设置页点「深色模式」chip 或清 force_dark 键。
- **深色文字整段消失根因（2026-08-16 修复，commit 90bba3b）**：M3 1.3.x（BOM 2024.12.01）的 `MaterialTheme` **不再提供 `LocalContentColor`**（Compose 默认值=Color.Black）→ 所有未显式指定颜色的 Text 在深色下渲染纯黑（浅色下黑字白底正常，故只影响深色；显式色的 chip/按钮文字不受影响）。修复：`NewAgeTheme` 的 CompositionLocalProvider 显式 `LocalContentColor provides effectiveScheme.onBackground`，并深色下强制中性文字色 onSurface/onBackground=0xFFE8ECF4、onSurfaceVariant=0xFFAEB8CC、outline=0xFF5E6A86（动态取色只保留主/次/第三色）。诊断方法：uiautomator dump 证明文字节点存在 → 像素扫描证明纯黑 → javap 反汇编 gradle 缓存 material3-android 1.3.1 aar 的 MaterialThemeKt 证明未提供 LocalContentColor（Surface 仍提供）。注意 `contentColorFor(ColorScheme)` 顶层重载在 1.3.1 签名变化，直接 provide onBackground 即可。
- **M3 底栏 50% 透光 + 取色风格完善（2026-08-16，commit 3cccfe0）**：① `AppRoot` LiquidGlassNavBar 的 M3 分支容器 alpha 0.95→0.5（玻璃主题分支 0.12 不动；普通黑/AMOLED 统一 0.5，滚动内容可透出，两种深色实测透出亮像素 548/562）；② 设置页「取色风格」从英文 chip 流改为列表：每行 = 按当前种子色+深浅实时生成的 primary/secondary/tertiary 三色圆点 + 英文名 + 中文说明 + 当前项主色对勾；九种风格说明文案在 SettingsScreen.kt 顶部 `paletteStyles`。
- **自定义实例目录 + Java/下载健壮化（2026-08-17，commit ed89822）**：① 设置→环境→「实例存储位置」：SAF OpenDocumentTree 选目录（先引导 MANAGE_EXTERNAL_STORAGE「所有文件访问」，vivo/MuMu 上 DocumentsUI 会先弹「允许访问此文件夹」确认）+ StorageDirUtil.treeUriToFile 解析 externalstorage tree URI 为真实路径 + 可写探测 + takePersistableUriPermission + rescanInstances（InstanceStore 注入 prefs，instancesRoot 读自定义路径，recoverFromDirs 直接识别所选目录既有服务端=「直接调用选中的游戏目录运行」；实测 MCTest/MyServer 端到端识别）。注意：MuMu 上 DocumentsUI 拒绝 Download 根目录（"无法使用此文件夹"），须进入子目录或新建目录才可选。② Java：删除加 AlertDialog 确认；下载加「取消」（Downloader.download shouldCancel 轮询）与失败「重试」；uninstall 清理 tar/tmp 残留且环境未就绪也能删。③ Downloader.downloadFromSources 增加 maxRounds/perSourceRetries/roundDelay 断网自动重试（断点保留），Java tar 解压失败回退内置 TarExtractor；实例 jar 下载用 .part 半成品（断点续传且不被目录扫描误识别）。④ setupEnv/removeInstance（先停服）/downloadAndCreate 异常保护。⑤ 主题磁贴「安卓原生液态玻璃」更名「液态玻璃」（AppThemeMode.GLASS.label）。
- **背景图裁剪（2026-08-17，commit a4a1d3c，用户"能不能做裁剪，参考广受好评项目"）**：集成 **CanHub Android-Image-Cropper**（`com.vanniktech:android-image-cropper:4.6.0`，Apache-2.0，uCrop 同源）——**4.6.0 只有 View 版**（`com.canhub.cropper.CropImageView`，无 Compose API/无 CropperView），走 **Activity 模式**：SettingsScreen 选图→复制 cacheDir/bg_crop_src.tmp→`FileProvider.getUriForFile(appContext, "${pkg}.cropper.fileprovider", file)`（库 manifest 自带 provider，cache-path 覆盖 cacheDir）→`CropImageContract().launch(CropImageContractOptions(uri, CropImageOptions()))`→回调 `result.getBitmap(appContext)`→`saveBackgroundImage(Bitmap)`。设置页「重设」按钮用同一 contract 重裁已存背景。**踩坑链**：① 曾先写自绘 Compose 裁剪页用 AndroidView 包 CropImageView → **ANR 死循环**（主线程卡 `AndroidViewHolder.onMeasure` measure 循环+持续 GC，MuMu 实测）→ 弃用自绘页，改 Activity 模式（删除 BackgroundCropScreen.kt）；② **CropImageActivity 必须 AppCompat 主题**：崩溃 `You need to use a Theme.AppCompat theme` → manifest 加 `<activity com.canhub.cropper.CropImageActivity android:theme="@style/Theme.KazeCrop" tools:replace="android:theme">`，Theme.KazeCrop parent=**Theme.AppCompat.Light**（⚠️ 不能 NoActionBar——NoActionBar 下无 ActionBar 菜单，"裁切"确认按钮消失无法提交）；③ `hasBackgroundImage` 原为 get() 读文件系统（非 State）→ 保存后 UI 不刷新（MuMu 实测"已保存但界面显示未选择"）→ 改为 `bgExists = mutableStateOf(...)`，save/clear 时更新。验证（MuMu）：选图→裁剪页（九宫格+L 角标记+右上"裁切"）→确认→设置页"已设置"→主页渐变图+模糊遮罩生效。**「重设」按钮已按用户要求移除（2026-08-17，commit aec7c9d）——已有背景图时再次点「选择背景图」即重新裁剪覆盖**（saveBackgroundImage 天然覆盖 bgFile）。
- **图标与文字颜色三档（2026-08-17，commit 353b669，用户"字体图标黑/白/跟随主题"）**：设置→背景图→「图标与文字颜色」chips（跟随主题/白色/黑色，prefs `fg_color_mode`）。实现：`FgColorMode` 枚举 + `LocalFgColorMode`（AppTheme.kt）；`NewAgeTheme` 加参数，在 effectiveScheme 之后按模式覆盖**中性前景角色** onSurface/onBackground/onSurfaceVariant/outline/outlineVariant（LIGHT=白系 D6DCE6，DARK=黑系 1A1C20）——**动态色底（chip/按钮容器）上的文字不翻转**（onPrimary 等保持，onPrimaryContainer 不动），避免对比失效；LocalContentColor 用 fgAdjusted.onBackground。MainActivity 读 prefs 传入。MuMu 实测：dark→主页文字变黑（背景渐变图）、按钮白字保持；light→文字/底栏图标变白、选中项保持主色；auto 恢复。测试后已恢复默认 auto。
- **裁剪页状态栏修复（2026-08-17，commit 2ca2d35，用户"顶部通知栏那边有问题"）**：Android 15 强制 edge-to-edge，CropImageActivity 图片内容延伸到状态栏后面 → 状态栏一片图片色（实测像素 (450,20)=R3G128B252 图片蓝）+ 图标看不清 + 蓝白断层。修复：Theme.KazeCrop 加 `android:statusBarColor=white` + `android:windowLightStatusBar=true`（白底深色图标）+ `android:windowOptOutEdgeToEdgeEnforcement=true`（内容不再顶到状态栏）+ `navigationBarColor=white/windowLightNavigationBar=true`。实测：状态栏白/图标黑、图片从工具栏下方开始。opt-out 只影响裁剪 Activity，主 app 的 enableEdgeToEdge 不受影响。
- **设置页精简 + 窄屏挤压修复（2026-08-17，commit fc2d53d）**：① 主题模式/深色样式/颜色来源三排选项 Row→FlowRow（真机 360dp 窄屏/系统大字下 chip 不再被挤压），CheckChip label 加 maxLines=1+Ellipsis；② 按用户要求删除设置页「部署」相关 UI：Linux 环境标题行+部署按钮、存放到外部存储开关、部署状态/进度/日志（envState/envItems/envLog/showEnvLog 全部移除，AppViewModel.setupEnv 仍被 HomeScreen 使用；环境部署已自动化：首次启动服务端时 DefaultServerManager 自动 env.setup+装 Java）；③ 「存储位置」独立成节（外观/存储位置/通用三区），路径+「选择目录」同行、恢复默认+说明在下方（初版按钮被长说明挤到屏外，已重排）。
- **真机启动失败修复（2026-08-17，commit bdbfa55，用户 manba/Java8 日志）**：① **下载内容校验**：镜像对不存在的大文件返回 200+HTML 错误页（华为 OpenJDK8 404→"下载完成"→tar 解压 0 字节）→ `Downloader.download/downloadFromSources` 加 `validate: (File)->Boolean`，RootfsJavaManager/ProotEnvironment 下载传 gzip 魔数+>1MB 校验，无效即删文件抛异常换源重试；② **rootfs 健康检查增强**：isReady 从"dash 可读"改为 rootfsHealthy()=dash+usr/bin/sh+usr/bin/apt-get 三者真实读取（FUSE 陈旧 dentry 需真实 read 触发），任一缺失视为损坏→isReady=false→下次启动自动重建（setup 重解压内置 asset）；重建后需重建 .l2s/sys/.empty（阶段 1 建、重解压会被删）；③ apt 回退前 `command -v apt-get` 前置检查（缺失报"rootfs 可能损坏将自动重建"），apt 失败错误信息去掉双层 "Java N 安装失败" 前缀。
- **运行日志持久化（2026-08-17，commit b64ca5a，用户"该实例暂无日志"反馈）**：系统消息（`> 检查 Linux 环境…/部署/Java/启动失败` 等 slot.log）原本只进内存 ConsoleStream，进程退出即丢、日志页看不到历史报错。现统一写入实例目录 `console-output.log`（与服务器 stdout 同一时间线，synchronized 防并发交错，超 8MB 轮转为 console-output.old.log）；日志页（LogsScreen）新增「运行日志」卡片展示该文件（含环境检查/Java 安装/启动报错完整记录），与「服务器日志 latest.log」「崩溃报告」并列。MuMu 实测启动失败全过程已落盘。
- **下载源修复（2026-08-17，commit 72d31fe，用户"源失败：下载内容校验失败"）**：华为镜像 adoptium/8 目录无该版本文件但返回 200（HTML 列表页无匹配）+ API 可能被墙 → ① `probeFastest` 读 1KB 嗅探响应，`<html`/`<?xml` 开头视为假 200 排除（不再被选为最快源）；② `RootfsJavaManager` 元数据改为**TUNA 目录页解析实际包名优先**（`https://mirrors.tuna.tsinghua.edu.cn/Adoptium/<v>/jdk/aarch64/linux/` 正则 `OpenJDK\d+U-jdk_aarch64_linux_hotspot_[^"<>]+\.tar\.gz`，已实测 8u502b07 97.9MiB 在列），Adoptium API 仅补充 GitHub 直链，华为目录页作兜底；③ 校验失败文案改为"返回内容不是有效文件（镜像可能下架了该版本），已换源重试"。
- **Zalith/FCL 版式移植（2026-08-14，用户"可以模仿FCL呀Zalith"，替换上一轮自创布局）**：主页=Zalith LauncherScreen RightMenu 启动面板（状态球+品牌为 avatar 位；当前实例选择器 VersionManagerLayout——图标28dp+名称+摘要跑马灯，点击弹 DropdownMenu，行点击=切换当前实例、行尾 ▶=直接启动；运行信息行 labelSmall alpha0.7；底部 56dp 全宽大启动按钮；导航全走底栏，快捷入口删除）；服务端=Zalith VersionsManageScreen（全页单张 BackgroundCard 画布；顶部横向滚动工具条=新建/导入 jar + 分类 chip 带数量「全部(N) 官方(N) 性能(N) 模组(N)」，按 coreType.category 筛选；列表项 VersionItemLayout=RadioButton 单选当前实例+34dp 图标+名称/摘要跑马灯+信息行 alpha0.7+启停钮+⋮菜单；FAB 已删）。设置页保持 BiliPai 体系不动。**踩坑：Compose 局部变量遮蔽——listOpen 声明两次导致点击设外层、DropdownMenu 读内层，下拉永不弹出（编译通过、无警告）**。参考源码 .tmp/ref/zl_versions_manage_screen.kt / zl_launcher_screen.kt / zl_versions_manage_elements.kt（GPL-3.0，已记 NOTICES）。

## Hyperframes 宣传片（2026-08-19，videos/kaze-slauncher-promo）

- **产物**：`videos/kaze-slauncher-promo/renders/video.mp4`（55s、1920x1080、30fps、h264+aac、6.8MB，含 18 条本地 SFX 音轨）。项目文件齐全：BRIEF.md / STORYBOARD.md（8 帧）/ frame.md（code-editorial preset 品牌 remix）/ compositions/frames/NN-*.html / index.html。
- **2026-08-19 二次渲染**：用户提供 6 张新真机截图（`C:\Users\yanzh\Downloads\Screenshot_20260819_195304.zip`，1260x2800 竖屏）替换展示图：02主页→home_new.jpg(195300)、03服务端列表→server_list_new.jpg(195304)、04版本选择→version_select.jpg(195322)、05主页状态球→home_new.jpg、06控制台→console_new.jpg(195307)、07左设置→settings_new.jpg(195312)+07右保留settings_glass_dark.png(深色玻璃对照)。新图 9:20 比旧图 9:16 窄长，各帧容器已调整为 378x840（07 卡 320x711）。195319(新建向导①核心类型选择)暂未入帧，可作替换候选。成片 7.8MB 已重渲染验证。
- **概念「终端流水诗」**（用户明确：参考视频 BV12BXvYEEx1 仅作品质基准，成片必须完全原创零相似）：深色终端窗口贯穿全片，命令逐行敲出「部署环境→装 Java→下载服务端→自动 EULA→上线→控制台」链路，每步完成右侧浮现真实 UI 截图（玻璃外框）。片尾 KAZE SLAUNCHER 字标 + `$ install Kaze SLauncher`。无旁白无 BGM（HeyGen 未登录 + MusicGen 依赖不可用 → `music: none`），节奏由 typing/key-press/riser/impact/ping/chime 等本地 SFX 驱动。
- **执行流程**：hyperframes 0.8.2（npx 缓存路径 `C:\Users\yanzh\AppData\Local\npm-cache\_npx\702923228c2ce1e6\node_modules\hyperframes\bin\hyperframes.mjs`，沙箱内 npx.cmd 被拒需用 node 直调）→ intent 层（route=product-launch-video, flow=automation, storyboard=no, autonomous）→ init → BRIEF → capture(no-capture path, 本地截图) → build-frame → STORYBOARD+Video direction → audio(BGM none+SFX) → 8 个并行 frame subagents → assemble-index → transitions inject → lint/check → snapshot → render。
- **关键踩坑**：
  1. **frame worker 中文编码损坏**：多个 worker 写出的 UTF-8 文件里中文变 GBK 乱码（含 `?` 破坏 JS 字符串语法 `Invalid or unexpected token`）。修复用 python `io.open(..., encoding='utf-8')` 行级替换（PowerShell 文本管道会二次乱码）。**教训：worker 写中文前必须确认 UTF-8；修中文文件用 read/edit 工具或 python，勿用 pwsh Get-Content/Set-Content**。
  2. **数字开头 id/class 选择器**：frame_id 如 `04-server` 作 id 前缀后，JS 里 `$("#04-server-t3")`/`querySelector("#07-themes-card-left .t7-card-pos")`/`.08-outro-letter` 抛 `is not a valid selector`。修复：改 `[id='04-server-t3']` 属性选择器（注意引号嵌套：外层双引号则内层单引号）或类名改字母开头。lint 的 `id_requires_css_escape` warning 在属性选择器下是误报，但**直接 `#数字` 选择器是运行时真错误**。
  3. **tl.set(..., 0) 初始隐藏**：`gsap_timeline_set_initial_hide` warning——t=0 的隐藏 set 在 playhead=0 不渲染，改 `gsap.set()`（timeline 外立即执行）。
  4. **frame.md 的 cream 被当 canvas**：assemble-index 用 `semanticColors` 把名为 cream/paper/canvas 的键当宿主背景色（浅色 #E3F2FD 透出导致收尾帧浅底）。深色主导项目需在 colors 块**最前面加 `canvas: "#0F1B33"`**（named 匹配按顺序，canvas 先命中）。
  5. **transitions inject 要求根 div 带 data-duration**：worker 根缺 data-start/data-duration 会报 `has no data-composition-id root`（实际是有但缺 duration）；修根后必须重新 assemble+inject。
  6. **assemble-index 轨道重叠**：同 track 时间重叠报 composition violation，需把重叠 clip 放不同 data-track-index。
  7. **pwsh 正则替换坑**：`-replace` 替换串里 `$1` 会被 PowerShell 展开、`\ ` 转义会写进文件；修 HTML 用 python 脚本文件更稳。
- **视觉模型**：本次读图用 agnes（`--provider agnes --base-url https://apihub.agnes-ai.cn/v1/ --model agnes-2.5-flash`，key 从 ~/.dsh/.credentials.yaml 的 AGNES_API_KEY 注入环境变量）。GLM/ZHIPU key 当前不存在于凭据文件；orcarouter config 的 base_url 优先级高于 --provider preset（需显式 --base-url）。

## 架构（多开）
- DefaultServerManager 已重构为多开会话：`ConcurrentHashMap<String, RuntimeSlot>`（每实例独立 Process/state/uptime/console），环境与 Java 安装用 AtomicBoolean 互斥；states 汇总流 `Map<instanceId, ServerState>`。
- 控制台每实例独立：`consoleFor(id)` 保留历史；AppViewModel 用 collectLatest 跟随 currentInstanceId 切换。
- `ServerProperties`：server.properties 读写 + `findFreePort`（25565 起自动分配，创建实例时写初始文件）+ `ensureInitial`。
- **新建服务端为全屏两段式流程页 `NewServerScreen`**（2026-08-14 用户要求推倒重做，旧 NewInstanceDialog 已删除）：阶段①核心类型大卡片网格（Zalith 加载器卡片风，导入 jar 直接触发文件选择）→ 阶段②搜索+正式/快照筛选+版本列表（FCL 风，整行可点，40 条上限提示）+ 配置卡（名称/内存/Java 覆盖/端口提示）+ 底部「下载并创建」。参考 FCL VersionInstallPage、Zalith SelectGameVersion/DownloadGame 系（GPL-3.0）。
- **Java 不内置（用户明确要求，2026-08-14）**：rootfs 为纯净 Ubuntu base；Java 8/17/21 通过设置页「Java 运行时（可选下载）」卡片按需 apt 安装/删除；新建实例可手动指定 Java 版本（自动推断默认，兼容性覆盖）。
- 附加组件：`ModrinthApi`（v2 开放 API 搜索/版本）+ `AddonManager`（plugins/mods 目录、*.jar.disabled 启停、CDN 下载安装）；AddonsScreen 支持 Paper 插件与 Fabric/Forge 模组（supports() 按核心类型门控）。
- 核心类型已全量解锁（Vanilla/Paper/Purpur/Spigot/Fabric/Forge/NeoForge/导入）。
- **「显示背景」开关仅在已选背景图时显示（2026-08-17，commit 1f390db，用户"清除了背景还能打开开关"）**：原开关行无条件显示，清除背景后仍可打开（无图无效果，逻辑矛盾）。改为开关行移入 `if (hasBackgroundImage)` 分支（与模糊/遮罩滑杆同级）；未选图时只显示"未选择背景图"文案。MuMu 实测：无图→无开关无清除；选图裁剪后→开关+清除+滑杆出现。
- **清除背景图后手风琴自动收起（2026-08-17，commit fa6c2b8，用户"做个动画，清楚了就自动收起来"）**：清除按钮 onClick 追加 `openSection = null`，AnimatedVisibility（`visible = openSection == "bg"`）自带淡出+高度折叠动画。MuMu 实测：点清除 → background.png 删除 + 手风琴整体收起（选择背景图/开关/滑杆/未选择文案全部折叠）。
- **「图标与文字颜色」设置项移动（2026-08-17，commit 6c8c519，用户"移到主题模式下边"）**：从背景图手风琴移到设置页外观区「主题模式」FlowRow 下方（顺序：主题样式→主题模式→图标与文字颜色→深色样式→颜色来源→取色风格），背景图手风琴只保留选择/清除/开关/模糊/遮罩。MuMu 实测位置与移除均正确。
- **Paper/Purpur 预置原版 jar 最终版（2026-08-17，commit e83872a，真机验证通过）**：旧版 paperclip 的下载目标**不是 versions/**——反编译 Paperclip.class + DownloadContext.class 确认：读 jar 内 `META-INF/download-context`（`<sha256>\t<url>\t<fileName>`，如 mojang_1.18.1.jar），`DownloadContext.download` 检查 `CWD/cache/<fileName>` 存在且 **SHA256 匹配**（`Util.isFileValid`）才跳过，否则无条件从 url（launcher.mojang.com）下载。另 paper 构建的 version.json id 与 mcVersion 可能不一致（paper-1.18.2-217.jar 的 version.json id=**1.18.1** → 需下 1.18.1 原版）。修复：`ensureVanillaJar` 解析 download-context → 下载（piston-meta/piston-data → BMCLAPI 兜底）到 `实例目录/cache/<fileName>` → SHA256 对比（不匹配删掉换源）→ paperclip 跳过联网。真机实测：预置 cache/mojang_1.18.1.jar → 服务器启动 → `Starting Minecraft server on *:25566` → `Preparing level "world"` ✓。**后续踩坑提醒：paperclip 版本/目录机制随 Paper 版本变化（bundler 新机制可能不同），遇下载失败先反编译 jar 内 Paperclip/DownloadContext 确认目标路径与校验方式。**
- **proot 缺失回退（2026-08-17，commit e37a34e，MuMu 环境部署"nativeLibraryDir 无 libproot.so"）**：APK 含 x86_64 lib（androidx.graphics.path.so）后，x86_64 首选设备（MuMu SUPPORTED_ABIS 首选 x86_64）安装只解压 x86_64 目录 → arm64 的 libproot.so/libproot-loader.so 不落盘（APK 内只有 lib/arm64-v8a/）。修复：ProotEnvironment.prootBinary/prootLoader 加回退 `extractNativeProot`——从 APK zip 提取 lib/arm64-v8a/ 到 filesDir/native-extract（模拟器等无 W^X 设备可 exec；真机 arm64 首选不会触发）。MuMu 验证时模拟器中途断开，待复验。
- **控制台「复制日志」按钮（2026-08-17/18，commit 78c80f2 + d5f4bd2 + 4461860，用户"保存日志左边加复制；复制跟控制台显示一模一样；导出也是控制台内容"）**：ConsoleScreen 右上角「保存日志」左侧新增复制按钮（ContentCopy 图标）→ `AppViewModel.copyConsoleLog()`。**v1 读 console-output.log 文件（与屏幕显示不一致）→ v2 改为复制 `_consoleLines.value` 内存流**（与控制台 LazyColumn 同一 StateFlow，含实时流/清空状态，逐行一致）；**「保存日志」saveConsoleLog 同步改为导出 _consoleLines 内容**（复制/导出/屏幕三者一致）；空日志不复制、Toast「暂无日志」。Main 线程 ClipboardManager.setPrimaryClip + Toast「已复制完整日志」。真机验证（服务器运行中）：连拍截图确认 Toast（vivo 上 `service call clipboard`/dumpsys 读不到剪贴板是权限限制，验证须抓 Toast 或 UI 粘贴）。
- **命令输入框 placeholder 修复（2026-08-18，commit fd97d58，用户"启动后提示消失，停止又出现"）**：OutlinedTextField 原无 enabled 限制——停止时也能输入（发送按钮禁用），输入文字顶掉 placeholder；且运行/停止切换 placeholder 文字不同。修复：① `enabled = serverState == Running`（停止时禁用输入框，placeholder「服务端运行后可输入命令」恒定可见）；② `LaunchedEffect(serverState)` 停止时清空 input 残留（placeholder 不再被文字顶掉）；③ 运行时 placeholder 显示「输入命令（stop / op 玩家名 / say …）」。
- **键盘弹出时输入框悬浮中部（2026-08-18，commit a4c4cd3，用户录屏复现）**：根因 = **窗口 resize + imePadding 双重压缩**——Android 15 强制 edge-to-edge 下未设 windowSoftInputMode 时系统默认 resize 窗口（2800→2110），ConsoleScreen 的 `imePadding()` 又消费一次 ime inset（690px），Column 被双重压缩到 ~1300，日志区被压扁（0-692）、输入框浮在中部（748-1028）、下方 600px 空白。修复：MainActivity manifest 加 `android:windowSoftInputMode="adjustNothing"`（窗口不再 resize，IME 只经 imePadding 单次处理）。真机验证（uiautomator dump + 像素采样）：修复前输入框 y748-1028、键盘顶 1560（间隙 532px）；修复后输入框 y1440-1720、键盘顶 1750（间隙仅 30px），头部/日志/底栏布局全部正常。**排查方法沉淀：键盘/IME 布局问题用 uiautomator dump 对比窗口 FrameLayout bounds（是否被 resize）+ EditText bounds + 像素采样三线交叉验证。**
- **控制台输入框与底部导航栏重叠（2026-08-18，commit 6fa89a9 + e6fe4fe，用户录屏复现）**：第一版误判为"静态重叠"（96dp→120dp，commit 6fa89a9），用户纠正：**真正问题是键盘收起动画时输入框下沉过头（穿过底栏）**。根因：`isImeVisible` 在收起动画期间（inset>0）仍为 true → padding 保持 0 → 输入框随 imePadding 动画沉到窗口底部，动画结束才弹回 96dp。**最终修复（e6fe4fe）**：不用 isImeVisible 开关，改为 **ime inset 联动**——`bottomPad = max(96dp - imeBottom, 0)`（imeBottom = `WindowInsets.ime.asPaddingValues().calculateBottomPadding()`），输入框位置恒 = 窗口底 - max(imeInset, 96dp)：键盘弹出贴键盘顶、收起动画中 inset 连续减小 padding 同步补偿、永不穿过底栏、平稳回到 96dp。用户实测 OK。**教训：IME 相关布局不要用 isImeVisible 布尔开关做瞬间切换，用 inset 连续值联动；animateDpAsState 也救不了时序问题。**
- **空服自动暂停默认关闭（2026-08-18，commit 9f9c304，用户日志实锤）**：用户 man 实例（MC 26.1.1）日志 1150 行显示 **Watchdog 崩溃重启循环 ×7**：`Server empty for 60 seconds, pausing` → 数分钟后 `Can't keep up! Running 3~77 秒 behind` → `Considering it to be crashed, server will forcibly shutdown` → app 自动重启。根因：MC 26 的 `pause-when-empty-seconds` 无键时默认 60s 自动暂停，proot 环境下暂停线程唤醒异常 → 卡死数万 ms → Watchdog 强杀。修复：① `ServerProperties.defaults` 模板写 `pause-when-empty-seconds=-1`（关闭，MC 官方语义）；② `ensureInitial` 对**已有文件缺失该键**自动补 -1（旧实例兼容；用户主动设过 >0 则保留）；③ 实例详情 UI 读取默认 "60"→"-1"（「关闭」chips 默认选中）。**教训：MC 1.21.2+ 新属性若对旧 server.properties 无键有隐式默认值，proot/Android 环境下要显式写键规避。**
- **导出日志 .log 被 vivo 改 .log.txt（2026-08-18，用户"你别导出.txt啊，要.log"）**：`CreateDocument("text/plain")` 在 vivo 上会把 .log 自动改 .log.txt（MIME 文本推断）。修复：改 `CreateDocument("application/octet-stream")`，vivo 不再改扩展名（构建中待验证）。
- **新建服务端流程重写（2026-08-20，用户"选择版本后直接开新页面配置（要有动画），不给用户选java，默认最优"）**：阶段②拆两独立页面，`AnimatedContent` 滑动切换（选中版本→配置页右滑入 300ms，返回反向；配置状态 name/内存提升到 VersionConfigPhase 保留）。版本列表页用 LazyColumn（整页空间，行尾箭头进入，RadioButton 选中态移除）；配置页=名称 + FCL 内存节 + **Java 只读行**（Build 图标 + "自动适配 Java N"）——javaMajorOverride 不再传（=0 自动推断），`CheckChip/BackgroundCard/RadioButton` 相关 import 已清理。端口号提升父级 `remember { findFreePort }` 传入（ConfigPage 无 viewModel 依赖）。
- **版本检测修复（2026-08-20，用户"paper 下载不是最新版提示 update，检测最新版，不局限 paper"）**：**根因 = fill.papermc.io v3 builds API 返回降序**（实测 1.21.11: first=132 last=34），原 `reversed()` 后取 firstOrNull = **最旧 build #34** → Paper 启动提示 update。修复：① `fetchPaperBuilds` 改 `sortedByDescending { it.id.toLongOrNull() ?: 0L }`；② `fetchVersions` 统一入口对**所有核心类型**做语义降序排序（versionKey 数字段逐位比较）+ distinctBy 去重（Purpur 原来根本没排序）。MuMu 实测版本列表 26.2→26.1.2→26.1.1→1.21.11 正确。
- **版本选择页 + 插件管理页优化（2026-08-20，用户"改改选择版本和插件选择，参考FCL"）**：① VersionRow 改 FCL 风——40dp 主色 12% 圆角图标方块（显示主.次版本号，如 26.2/1.21）+ titleSmall SemiBold 版本号 + 徽标 + 箭头；② AddonsScreen 重写——去 BackgroundCard 大卡（直铺背景+分隔线），ModrinthSearchHit 加 `icon_url` 字段（v2 search 自带），自写 RemoteIcon 异步加载（URL+BitmapFactory，无图片库；失败显示绿方块 M 占位），下载数格式化 1.2K/3.4M，已安装行加 42dp 图标方块（启停变色）。MuMu 实测：图标加载正常、结果行信息密度合适。
- **UI 全面走查 + 优化（2026-08-20，用户"全面优化软件界面"）**：真机（vivo）+ MuMu（x86_64，900x1600，adb 127.0.0.1:16384）逐页截图走查（read_image 直读，当前模型 Flash-Vision-Exp 支持图像）。改动：① CoreCard 统一 height(118.dp)+SpaceBetween（描述 1/2 行时网格整齐）；② 版本列表 LazyColumn top 4→8dp（首项不贴 chips）；③ ThemePreview M3 白卡加边框+主色蓝点+底色 E9EDF3（原 F5F7FA 对比弱）。**走查结论：服务端页信息行"重叠"是缩图伪影非 bug；分类 chips 溢出是 horizontalScroll 正常行为；主页黑带为用户背景图特例。** MuMu 截屏注意：`adb shell screencap -p /sdcard/x.png` + `pull`（exec-out 经 pwsh/cmd 管道会 UTF-16 损坏，已知坑）；uiautomator dump 定位坐标再 tap。
- **GitHub Release 发布/替换（2026-08-20）**：仓库 `0Sakura721/Kaze-SLauncher`，凭据 = Windows 凭据管理器 `git:https://github.com`（gho_ OAuth token，`git credential fill` 取出，勿写记忆）。已有 release id=372165127 tag=v0.1.0（原为 **prerelease=true → /releases/latest 恒 404，PATCH prerelease=false 后恢复，注意 GitHub 缓存延迟约 15s**）。替换 asset：DELETE `/releases/assets/{id}` → POST uploads.github.com `/releases/{id}/assets?name=...`（ContentType application/vnd.android.package-archive，81MB 成功）。asset 名 `Kaze-SLauncher-v0.1.0-arm64.apk`。
- **检查更新（2026-08-20，用户"设置里加检查更新，下载源和方式同 operit.app"）**：`core/update/UpdateChecker.kt`——版本走 `api.github.com/repos/0Sakura721/Kaze-SLauncher/releases/latest`（GitHub 仓库见 README），下载 = GitHub 原链 + operit.app 同款加速镜像前缀列表（moeyy.xyz/mirror.ghproxy.com/hub.gitmirror.com/boki.moe/ednovas/limoruirui/abskoop/tbedu/llkk/nxnow/monkeyray/mrhjx/zwy 等），复用 `Downloader.downloadFromSources`（并发测速选最快+失败回退+断点续传）；validate=APK 魔数 PK\x03\x04+>1MB。UI：设置→通用「检查更新」手风琴（守护与电池和关于之间），当前版本号取 packageManager（无 BuildConfig）；下载到 cacheDir/updates → 新 FileProvider（`${applicationId}.fileprovider` + res/xml/file_paths.xml cache-path）→ ACTION_VIEW 安装（REQUEST_INSTALL_PACKAGES 已有）。**踩坑：Downloader 的 onProgress 回调非 suspend，不能 withContext，跨线程直接写 MutableState 即可（snapshot 线程安全）；File.readBytes() 无参，魔数用 inputStream.read(ByteArray(4))**。
- **启动提速 + 控制台/原始日志一致性（2026-08-20，用户"启动特别慢；日志和控制台显示的一样"）**：① `javaArgs`：Xms=Xmx（消堆扩容停顿）、`-XX:+PerfDisableSharedMem`（禁 hsperfdata 写 /tmp）、`-XX:MaxGCPauseMillis=200`、`-Djava.security.egd=file:/dev/urandom`（老 JDK SecureRandom 阻塞规避）；② `ServerProperties.defaults` view-distance 10→8、simulation-distance 10→6（世界准备随距离²，仅新实例，详情页可调回）；③ **\r 进度行**：stdout 消费改逐字符流式（readLine 会把 \r 当分隔符），`\r` → `ConsoleStream.emitReplace`（控制台替换上一行不刷屏）+ 文件仍逐行落盘（与 latest.log 一致）；新增 `slot.logReplace`。若仍慢需真机 console-output.log 定位瓶颈。
- **启动即保活（2026-08-20，用户"一启动服务端就保活"）**：ServerGuardService 原只在 launchServer（进程真正起来）时启动——环境部署/Java 安装可能耗时数分钟，期间退后台会被杀。改为 `start()` 开头 `setState(Starting)` 后立即 `startGuard`；启动失败 catch 且无其他存活进程时 `ServerGuardService.stop`（防保活残留）。
- **默认关正版验证 + 实例改名独立于 MOTD（2026-08-20，用户"默认关闭正版验证；MOTD和软件里的名字可独立修改"）**：① `ServerProperties.defaults` online-mode true→**false**（新实例默认离线可进服；实例详情页开关可改回，ensureInitial 不覆盖已有键）；② `InstanceStore.rename(id,newName)`（copy 替换 + save，只改显示名不动目录）+ `AppViewModel.renameInstance`；③ InstanceDetailScreen 顶部栏加 Edit 图标按钮 → AlertDialog 改名（提示"仅修改软件内显示名；MOTD 独立保存在 server.properties"）。MOTD 编辑器早已存在（编辑器首行"服务器标题（MOTD）"，value 缺省回退实例名但键存在即独立）。
- **自动分配内存只读 + 下限提升（2026-08-20，用户"自动分配时内存不可手动改；逻辑不行，太小开不了"）**：自动模式滑块 `enabled=false`（显示系统建议值）、精确输入按钮禁用；effectiveMb=自动=suggestMb（删 lowerBoundMb 状态）；suggestMb 下限 512→**1024MB**（512 连原版服都起不来），上限 8192 不变；文案"自动分配 X GB · 根据设备可用内存，不可手动修改"。
- **新建服务端配置卡内存改 FCL 风格（2026-08-20，用户"内存设置搬FCL的"→"直接搬过来，重点优化这个界面"→真机 46083 调试修正）**：NewServerScreen VersionConfigPhase 整体优化：① **配置卡重排分节**（实例名称 / 游戏内存 / Java 版本 / 端口，HorizontalDivider 分隔，标题行显示「配置 + MC 版本 · 核心类型」）；② **AnimatedVisibility 淡入**（选中版本后配置卡出现）；③ **VersionRow 优化**（选中 primary alpha 0.10 背景高亮 + 名称 SemiBold + 正式/快照圆角徽标）；④ **游戏内存 FCL 完整布局**：「游戏内存」标题 + 「自动分配内存/手动分配」Switch（默认自动）→ 滑块标签（auto→「最低内存分配」/ manual→「游戏分配」）→ FCLNumberSeekBar 滑块（thumb 64×30dp 圆角色块显示 %.1f GB 数值）→ 状态文案（FCL strings 原文："最低分配 X.X GB / 实际分配 X.X GB"、"游戏分配 X.X GB"、超设备 80% 红字"（设备仅 X.X GB 可用）"）→ 设备信息行（"设备中已使用 X.X GB / 设备总内存 Y.Y GB"）→ 范围提示 + 「精确输入」TextButton（弹 AlertDialog 输入 GB）。参考 .tmp/ref/fcl_number_seekbar.java、fcl_strings_zh.xml（FCL-Team/FoldCraftLauncher@main 经 jsdelivr 抓取；FCL 全局游戏设置布局文件 page_setting_global.xml 在该仓库不存在/改名，strings+控件源码已够重建）。**真机调试修正（2026-08-20，uiautomator dump + tap/swipe 验证）**：① **配置卡原在 102 个版本列表之后，选版本后要滚很久才看到——移到版本列表之前**（搜索/chips 下方，选中即见）；② **自动分配建议数学 bug**：suggestMb 公式 `(availMemGb*0.5/256).toInt()*256` 中 availMemGb 是 GB 忘 ×1024 → 恒 512MB → 实际分配恒 1.0GB；修 `((availMemGb*1024f*0.5f)/256f).toInt()*256f`；③ **真机 ActivityManager.getMemoryInfo().availMem 返回异常=totalMem**（已使用显示 0.0）→ 改读 /proc/meminfo 的 MemTotal/MemAvailable（File.readLines 解析 kB）；④ **fmtGb 单位 bug**：fmtGb 输入是 MB（内部 /1024 显示 GB），usedMemGb 已是 GB 再传 → 显示 0.0 GB；修 `fmtGb(usedMemGb*1024f)`；⑤ **thumb 内 clickable 会破坏滑块拖动**（真机 tap/swipe 无效）→ 移除，精确输入靠按钮。真机实测通过：自动分配 2.8GB（availMem 5.8GB×0.5）、手动模式切换、滑块点击跳转（y 坐标须用 thumb 文本中心 ~1608 而非轨道估算）、精确输入对话框、已使用 9.2/总 14.9GB。**踩坑①：material3 1.3.1 的 KeyboardOptions 在 `androidx.compose.foundation.text` 包（非 ui.text.input）**——javap OutlinedTextField 签名证实。**踩坑②：compose pointer API（awaitEachGesture/awaitFirstDown/positionChanged）版本混乱勿依赖。**踩坑③：cmd `set VAR=path && ...` 会把 && 前空格带进值 → 路径尾空格致 Gradle lock 目录创建失败，须 `set "VAR=path"` 引号包裹。**踩坑④：uiautomator 定位 Slider 交互坐标看 thumb 文本节点中心 y，别用轨道估算。**

## 待办（全面完善路线，目标 goal-a807f30d）
- 已全部实现（2026-08-14）：①多开 ②实例详情+server.properties 编辑器 ③插件管理（Modrinth）④模组管理 ⑤备份/恢复/**导入导出**（SAF CreateDocument/OpenDocument）⑥**玩家管理**（ConsoleParser 解析 list 响应 + join/leave 事件 + OP/白名单/踢出快捷命令）⑦**崩溃报告 + latest.log 查看页**（LogsScreen，读文件末尾 300KB）。
- 模拟器已验证：备份 52MB zip 创建/恢复/导出按钮、日志页空态、玩家管理卡渲染；Java 安装+服务端运行仍受**模拟器 app-data 存储损坏**阻塞（应用代码在 18:59/19:29 两次环境部署全通过）。
- 踩坑：Kotlin 嵌套注释——注释里写 `usr/*`、`crash-reports/*.txt` 都会吞掉后续代码（"Unclosed comment"）。
- 参考源码已存 .tmp/ref/：Zalith LauncherScreen/VersionsManageElements（VersionItemLayout 卡片）、FCL VersionListItem/ProfileListAdapter/VersionInstallPage。
