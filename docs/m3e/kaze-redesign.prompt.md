请用 Material 3 Expressive 的设计实现KAZE SLauncher 界面重构。Android 上运行 Minecraft Java 服务端的启动器。核心链路：部署自包含 Linux 环境（proot + Ubuntu rootfs）→ 按 MC 版本自动安装 Java → 下载或导入服务端核心 → 首次启动自动处理 eula.txt → 实时控制台看日志并输入命令。数据模型是「多实例」：每个服务端实例有独立的目录、Java 版本、内存上限、控制台日志与运行状态。这是对既有应用的一次界面重构：后端、功能与常驻底栏全部保留，只重做其余界面层。
目标为竖屏手机（412×892dp），同时支持浅色和深色模式，并跟随设备的系统设置切换。
实现目标是 Android（原生应用）。
下面的屏幕结构是传达意图的草图，不是最终规格。不要把它当静态图片照搬，而要做成这类应用通常应具备的功能齐全、真正可用的成品。

## 配色
使用动态配色：在 Android 12 及以上应用由用户壁纸生成的配色方案（dynamicLightColorScheme / dynamicDarkColorScheme），不支持的设备则使用下面的颜色作为备用。
备用主题为 Purple 系。请在Material 3 的浅色和深色配色方案中设置以下颜色，UI 的所有颜色都通过这些角色引用。
浅色配色：
- primary #6750A4 / onPrimary #FFFFFF / primaryContainer #EADDFF / onPrimaryContainer #21005D
- secondary #635A75 / secondaryContainer #E8DEF8 / onSecondaryContainer #1D192B / tertiaryContainer #FFD8E4 / onTertiaryContainer #31111D
- surface #FEF7FF / surfaceContainerLow #F7F2FA / surfaceContainer #F3EDF7 / surfaceContainerHigh #ECE6F0 / surfaceContainerHighest #E6E0E9
- onSurface #1D1B20 / onSurfaceVariant #49454F / outline #79747E / outlineVariant #CAC4D0
- inverseSurface #322F35 / inverseOnSurface #F5EFF7 / inversePrimary #D0BCFF
- error #B3261E / onError #FFFFFF / errorContainer #F9DEDC / onErrorContainer #410E0B
深色配色：
- primary #D2BCFC / onPrimary #32226F / primaryContainer #4C3889 / onPrimaryContainer #E9DDFF
- secondary #CDC1E1 / secondaryContainer #4B425D / onSecondaryContainer #E9DDFD / tertiaryContainer #6C3644 / onTertiaryContainer #FDDAE1
- surface #141317 / surfaceContainerLow #1C1B1F / surfaceContainer #201F23 / surfaceContainerHigh #2B292D / surfaceContainerHighest #363438
- onSurface #E4E1E7 / onSurfaceVariant #C9C4D1 / outline #938F9B / outlineVariant #494550
- inverseSurface #E4E1E7 / inverseOnSurface #313034 / inversePrimary #6750A4
- error #F2B8B5 / onError #601410 / errorContainer #8C1D18 / onErrorContainer #F9DEDC

## 形状、字体与动效
- 圆角沿用 M3 Expressive 的默认值（按钮为胶囊形，卡片 20dp，对话框 28dp）。
- 字体使用 Roboto Flex。标题、按钮文字和标签页使用 M3 Expressive 的 emphasized 字体样式（headlineMediumEmphasized 等更粗的字重）。
- 动效使用 MotionScheme.expressive()：屏幕过渡和状态变化带轻微回弹的弹簧效果。

## 屏幕结构
共有 9 个屏幕：“主页”、“服务端”、“控制台”、“实例详情”、“新建服务端”、“插件与模组”、“启动日志”、“外观设置”、“运行与存储”。

打开应用后的落地屏：一眼看到「服务器现在是什么状态」，并且可以立刻启停。
“主页”屏幕从上到下依次如下（重叠的组件会特别说明）：
- 上部靠左，从左到右横向排成一行：M3 Expressive 形状变化的加载指示器、“运行中 02:14:37”标签片（选中状态）（放在同一行并垂直居中，不要竖着堆叠或换行）。
- 上部放置浮起卡片（高 112dp）。标题“生存服”，正文“Paper 1.21.4 · Java 21 · 4096 MB · 端口 25565 · 在线 0 人 · 内存 1.8 / 4.0 GB”。
- 中部靠左放置由“启动服务端”(填充)“停止”(色调)这 2 个按钮横向相连组成的按钮组。
- 中部放置描边卡片（高 88dp）。标题“已运行 02:14:37”，正文“在线 3 人 · 内存 1.8 / 4.0 GB”。
- 中部放置“Linux 环境已就绪”（辅助文本“proot + Ubuntu 24.04 · 已安装 Java 17 / 21”），左侧显示 check_circle 图标（背景 primaryContainer）。
- 下部放置4个项目的导航栏（“主页”(home)、“服务端”(dns)、“控制台”(terminal)、“设置”(settings)，第一项为选中状态）。

服务端实例的列表与管理：多开、筛选、启停、导入。
“服务端”屏幕从上到下依次如下（重叠的组件会特别说明）：
- 上部靠左，从左到右横向排成一行：“新建”的填充按钮（带 add 图标）、“导入 jar”的色调按钮（带 file_open 图标）（放在同一行并垂直居中，不要竖着堆叠或换行）。
- 上部居中放置由“全部 3”(选中)“官方 1”“性能 1”“模组 1”横向排列组成的标签片组。
- 上部放置“生存服”（辅助文本“Paper 1.21.4 · Java 21 · 4096 MB · 运行中”），左侧显示 dns 图标（背景 primaryContainer）。
- 中部放置“创造服”（辅助文本“Paper 1.20.4 · Java 17 · 2048 MB · 已停止”），左侧显示 dns 图标（背景 secondaryContainer）。
- 中部放置“模组服”（辅助文本“Fabric 1.21.1 · Java 21 · 6144 MB · EULA 未接受”），左侧显示 extension 图标（背景 tertiaryContainer）。
- 中部靠左放置由“启动全部”(填充)“停止全部”(色调)这 2 个按钮横向相连组成的按钮组。
- 下部放置4个项目的导航栏（“主页”(home)、“服务端”(dns)、“控制台”(terminal)、“设置”(settings)，“服务端”为选中状态）。

实时日志与命令输入：这是这个应用的主工作台。
“控制台”屏幕从上到下依次如下（重叠的组件会特别说明）：
- 上部靠左放置“运行中”标签片（选中状态）。
- 中部放置填充卡片（高 500dp）（背景 inverseSurface）。标题“[12:04:31 INFO]: Preparing level "world"
[12:04:33 INFO]: Preparing spawn area: 46%
[12:04:35 INFO]: Done (4.812s)! For help, type "help"”。
- 中部放置标签为“输入命令…”的描边文本输入框。
- 下部放置4个项目的导航栏（“主页”(home)、“服务端”(dns)、“控制台”(terminal)、“设置”(settings)，“控制台”为选中状态）。

一个服务端的全部控制项：运行、配置、世界、备份、日志、附加组件。
“实例详情”屏幕从上到下依次如下（重叠的组件会特别说明）：
- 上部靠左放置粗体文本“生存服”（22sp）。
- 上部放置“运行”、“配置”、“世界”、“附加”这 4 个标签页（第一项为选中状态）。
- 中部放置浮起卡片（高 112dp）。标题“运行状态”，正文“已运行 02:14:37 · 在线 3 人 · 内存 1.8 / 4.0 GB · 端口 25565”。
- 中部放置波浪形线性进度条（45%）。
- 中部放置“自动备份”（辅助文本“每 30 分钟 · 最近 今天 12:00 · 共 4 份”），左侧显示 history 图标（背景 secondaryContainer）。
- 中部放置“查看启动日志”（辅助文本“每次启动的完整输出与退出码”），左侧显示 receipt_long 图标（背景 secondaryContainer）。
- 中部放置“插件 / 模组”（辅助文本“从 Modrinth 搜索并安装”），左侧显示 extension 图标（背景 secondaryContainer）。
- 中部放置“停止服务端”的填充按钮（带 stop 图标）。
- 下部放置4个项目的导航栏（“主页”(home)、“服务端”(dns)、“控制台”(terminal)、“设置”(settings)，“服务端”为选中状态）。

三步向导：选核心 → 选版本与资源 → 确认安装。
“新建服务端”屏幕从上到下依次如下（重叠的组件会特别说明）：
- 上部靠左放置粗体文本“新建服务端”（22sp）。
- 上部放置波浪形线性进度条（33%）。
- 上部放置填充卡片（高 96dp）。标题“Vanilla 官方原版”，正文“Mojang 官方 · 兼容性最好”。
- 中部放置描边卡片（高 96dp）。标题“Paper 性能优化”，正文“插件生态最好 · 性能更强”。
- 中部放置描边卡片（高 96dp）。标题“导入自定义 jar”，正文“已经有 server.jar？直接导入”。
- 中部放置描边卡片（高 96dp）。标题“可选项（建服时就定下来）”，正文“端口 25565 · 最大玩家 20 · 正版验证 开 · 游戏模式 生存”。
- 中部靠左放置“下一步”的填充按钮（带 arrow_forward 图标）。
- 下部放置4个项目的导航栏（“主页”(home)、“服务端”(dns)、“控制台”(terminal)、“设置”(settings)，“服务端”为选中状态）。

从 Modrinth 搜索并一键安装到当前实例。
“插件与模组”屏幕从上到下依次如下（重叠的组件会特别说明）：
- 上部靠左放置粗体文本“插件”（22sp）。
- 上部放置占位文字为“搜索 Modrinth”的搜索栏。
- 上部居中放置由“适合 1.21.4”(选中)“服务端”“下载最多”横向排列组成的标签片组。
- 中部放置“LuckPerms”（辅助文本“权限管理 · 128M 下载”），左侧显示 key 图标（背景 primaryContainer）。
- 中部放置“EssentialsX”（辅助文本“基础指令集 · 96M 下载”），左侧显示 handyman 图标（背景 secondaryContainer）。
- 中部放置“WorldEdit”（辅助文本“地形编辑 · 210M 下载”），左侧显示 brush 图标（背景 tertiaryContainer）。
- 中部靠左放置“安装所选”和 download 图标的扩展 FAB（色调）。
- 下部放置4个项目的导航栏（“主页”(home)、“服务端”(dns)、“控制台”(terminal)、“设置”(settings)，“服务端”为选中状态）。

每次启动的历史记录：退出码、耗时、失败原因。
“启动日志”屏幕从上到下依次如下（重叠的组件会特别说明）：
- 上部靠左放置粗体文本“启动日志”（22sp）。
- 上部靠左放置由“全部”(选中)“失败”横向排列组成的标签片组。
- 上部放置“今天 12:04 · 成功”（辅助文本“启动耗时 4.8s · 退出码 0 · Java 21”），左侧显示 check_circle 图标（背景 primaryContainer）。
- 中部放置“今天 09:31 · 失败”（辅助文本“UnsupportedClassVersionError · 需要 Java 21，当前 17”），左侧显示 error 图标（背景 tertiaryContainer）。
- 中部放置“昨天 21:12 · 成功”（辅助文本“启动耗时 5.1s · 退出码 0 · Java 21”），左侧显示 check_circle 图标（背景 primaryContainer）。
- 中部靠左放置由“导出全部”(填充)“清空记录”(文字)这 2 个按钮横向相连组成的按钮组。
- 下部放置4个项目的导航栏（“主页”(home)、“服务端”(dns)、“控制台”(terminal)、“设置”(settings)，“服务端”为选中状态）。

主题、颜色与字体：应用长什么样。
“外观设置”屏幕从上到下依次如下（重叠的组件会特别说明）：
- 上部放置浮起卡片（高 112dp）。标题“外观”，正文“主题模式 · 颜色来源 · 玻璃强度”。
- 上部放置“主题模式”（辅助文本“跟随系统”），左侧显示 brightness_6 图标（背景 secondaryContainer）。
- 中部放置“颜色来源”（辅助文本“壁纸动态取色”），左侧显示 colorize 图标（背景 secondaryContainer）。
- 中部放置“深色样式”（辅助文本“普通黑”），左侧显示 dark_mode 图标（背景 secondaryContainer）。
- 中部放置浮起卡片（高 88dp）。标题“字体与动效”，正文“强调字重 · 动效方案”。
- 中部放置“标题与标签使用强调字重”开关（初始状态为开）。
- 中部放置“表现力弹簧动效”开关（初始状态为开）。
- 下部放置4个项目的导航栏（“主页”(home)、“服务端”(dns)、“控制台”(terminal)、“设置”(settings)，“设置”为选中状态）。

实例目录、电池优化、Java、更新通道与许可证。
“运行与存储”屏幕从上到下依次如下（重叠的组件会特别说明）：
- 上部放置浮起卡片（高 88dp）。标题“存储”，正文“实例目录 · 占用空间”。
- 上部放置“实例目录”（辅助文本“默认位置 · 已用 2.4 GB”），左侧显示 folder_open 图标（背景 secondaryContainer）。
- 中部放置“已安装的 Java”（辅助文本“Java 17 · Java 21”），左侧显示 coffee 图标（背景 secondaryContainer）。
- 中部放置浮起卡片（高 88dp）。标题“后台与更新”，正文“电池优化 · 更新通道 · 自动检查”。
- 中部放置“电池优化”（辅助文本“已加入白名单”），左侧显示 battery_saver 图标（背景 secondaryContainer）。
- 中部放置“启动时自动检查更新”开关（初始状态为开）。
- 中部放置标签为“更新通道”的填充下拉菜单（点击展开菜单选择一项，选项为，初始未选择）（左侧显示 alt_route 图标），辅助文本为“预览版”。
- 中部放置“关于与许可证”的描边按钮（带 gavel 图标）。
- 下部放置4个项目的导航栏（“主页”(home)、“服务端”(dns)、“控制台”(terminal)、“设置”(settings)，“设置”为选中状态）。

## 行为与屏幕跳转
- 导航栏：点击“主页”项后以淡入的方式跳转到“主页”屏幕；点击“服务端”项后以淡入的方式跳转到“服务端”屏幕；点击“控制台”项后以淡入的方式跳转到“控制台”屏幕；点击“设置”项后以淡入的方式跳转到“外观设置”屏幕；常驻底栏：**这一处保留现有设计，本次重构不动它**。列在草图里只为了确认它相对内容的位置与高度。
- 加载指示器：服务状态指示：运行中缓慢旋转，启动中加速，停止时静止成一个固定造型。
- “运行中 02:14:37”标签片：状态胶囊：状态色圆点 + 已运行时长。
- “生存服”卡片：当前实例卡：点一下展开实例列表切换，卡片本身就是选择器（取代原来的下拉菜单）。正文那行带上「在线人数 / 内存 / 端口」，所以这一屏不再单独放一张运行概况卡。
- “启动服务端”按钮：点击后以放大的方式跳转到“控制台”屏幕；主行动：停止状态下是「启动服务端」；启动过程中变成「取消启动」，全程可点，用户在任何阶段都能中止。
- “停止”按钮：只在运行中或启动中可点。
- “已运行 02:14:37”卡片：运行时长用大号等宽数字，一眼可读；服务器没跑时显示「未运行」，不要显示 00:00:00。
- “Linux 环境已就绪”列表项：环境状态行：未部署时行尾出现「部署」按钮；部署中这一行变成波浪进度条 + 当前步骤文案。
- 导航栏：点击“主页”项后以淡入的方式跳转到“主页”屏幕；点击“服务端”项后以淡入的方式跳转到“服务端”屏幕；点击“控制台”项后以淡入的方式跳转到“控制台”屏幕；点击“设置”项后以淡入的方式跳转到“外观设置”屏幕；常驻底栏：**这一处保留现有设计，本次重构不动它**。列在草图里只为了确认它相对内容的位置与高度。
- “新建”按钮：点击后以从底部滑入的方式跳转到“新建服务端”屏幕；打开新建向导。
- “导入 jar”按钮：用系统文件选择器挑一个已有的 server.jar 导入。
- “全部 3”标签片：分段筛选的选中项。
- “官方 1”标签片：Vanilla / Paper。
- “性能 1”标签片：Purpur / Spigot。
- “模组 1”标签片：Fabric / Forge / NeoForge。
- “生存服”列表项：点击后以从右侧滑入的方式跳转到“实例详情”屏幕；实例行：点开实例详情；左侧是运行状态徽标；行尾是启停按钮与 ⋮ 菜单。
- “模组服”列表项：EULA 未接受的实例必须在行内直接点出来，不能藏进详情页。
- “启动全部”按钮：启动当前筛选出的全部实例。
- “停止全部”按钮：停止全部运行中的实例。
- 导航栏：点击“主页”项后以淡入的方式跳转到“主页”屏幕；点击“服务端”项后以淡入的方式跳转到“服务端”屏幕；点击“控制台”项后以淡入的方式跳转到“控制台”屏幕；点击“设置”项后以淡入的方式跳转到“外观设置”屏幕；常驻底栏：**这一处保留现有设计，本次重构不动它**。列在草图里只为了确认它相对内容的位置与高度。
- “运行中”标签片：状态胶囊，带状态色圆点。
- “[12:04:31 INFO]: Preparing level "world"
[12:04:33 INFO]: Preparing spawn area: 46%
[12:04:35 INFO]: Done (4.812s)! For help, type "help"”卡片：终端面板：等宽字体，按日志级别着色（INFO 用高对比浅色、WARN 琥珀、ERROR 红、命令回显蓝）。这是「终端画布」而不是普通卡片，除圆角外不要加装饰。新日志到达时自动滚到底；用户手动上滑就暂停跟随，并出现「回到底部」按钮。
- “输入命令…”文本输入框：命令输入：服务端没运行时禁用，占位文字换成「服务端运行后可输入命令」。
- 导航栏：点击“主页”项后以淡入的方式跳转到“主页”屏幕；点击“服务端”项后以淡入的方式跳转到“服务端”屏幕；点击“控制台”项后以淡入的方式跳转到“控制台”屏幕；点击“设置”项后以淡入的方式跳转到“外观设置”屏幕；常驻底栏：**这一处保留现有设计，本次重构不动它**。列在草图里只为了确认它相对内容的位置与高度。
- 文本“生存服”：实例名，点一下可以重命名。
- 标签页：实例内的四个分区：运行状态 / server.properties / 世界与备份 / 插件与模组。
- “运行状态”卡片：状态卡：左侧大号运行时长，右侧内存占用；内存超过 80% 时换成 error 色。
- “内存占用”线性进度条：内存占用，波浪进度条；超过 80% 换成 error 色。
- “查看启动日志”列表项：点击后以从右侧滑入的方式跳转到“启动日志”屏幕。
- “插件 / 模组”列表项：点击后以从右侧滑入的方式跳转到“插件与模组”屏幕。
- “停止服务端”按钮：主行动：运行中是「停止服务端」，已停止是「启动服务端」。
- 导航栏：点击“主页”项后以淡入的方式跳转到“主页”屏幕；点击“服务端”项后以淡入的方式跳转到“服务端”屏幕；点击“控制台”项后以淡入的方式跳转到“控制台”屏幕；点击“设置”项后以淡入的方式跳转到“外观设置”屏幕；常驻底栏：**这一处保留现有设计，本次重构不动它**。列在草图里只为了确认它相对内容的位置与高度。
- 文本“新建服务端”：向导标题。
- “第 1 步，共 3 步”线性进度条：向导进度：三步分别是「选核心」「选版本与资源」「确认安装」。
- “Vanilla 官方原版”卡片：选中的核心：主色容器色底 + 右上角对勾，三选一。
- “可选项（建服时就定下来）”卡片：这一组是可选覆盖项：留空就用默认值，省得建完再进实例详情改。
- “下一步”按钮：第一步必须选中一个核心才能点。
- 导航栏：点击“主页”项后以淡入的方式跳转到“主页”屏幕；点击“服务端”项后以淡入的方式跳转到“服务端”屏幕；点击“控制台”项后以淡入的方式跳转到“控制台”屏幕；点击“设置”项后以淡入的方式跳转到“外观设置”屏幕；常驻底栏：**这一处保留现有设计，本次重构不动它**。列在草图里只为了确认它相对内容的位置与高度。
- 文本“插件”：标题：插件 / 模组，跟着实例的核心类型变。
- “搜索 Modrinth”搜索栏：搜索框：回车发起搜索，搜索中变成加载指示器。
- “适合 1.21.4”标签片：按当前实例的 MC 版本过滤。
- “服务端”标签片：只显示服务端可用的项目。
- “下载最多”标签片：排序方式。
- “LuckPerms”列表项：搜索结果行：点开项目详情；行尾是安装按钮，安装中变成进度。
- “安装所选”扩展 FAB：批量安装；下载中显示波浪进度，并且可以取消。
- 导航栏：点击“主页”项后以淡入的方式跳转到“主页”屏幕；点击“服务端”项后以淡入的方式跳转到“服务端”屏幕；点击“控制台”项后以淡入的方式跳转到“控制台”屏幕；点击“设置”项后以淡入的方式跳转到“外观设置”屏幕；常驻底栏：**这一处保留现有设计，本次重构不动它**。列在草图里只为了确认它相对内容的位置与高度。
- 文本“启动日志”：标题。
- “全部”标签片：筛选：全部。
- “失败”标签片：只看启动失败的那几次。
- “今天 12:04 · 成功”列表项：一次启动一行，图标和颜色直接表达成功 / 失败，不要让用户点进去才知道结果。
- “今天 09:31 · 失败”列表项：失败原因直接写在摘要里。
- “导出全部”按钮：打包导出为一个 txt 或 zip。
- “清空记录”按钮：清空历史，必须二次确认。
- 导航栏：点击“主页”项后以淡入的方式跳转到“主页”屏幕；点击“服务端”项后以淡入的方式跳转到“服务端”屏幕；点击“控制台”项后以淡入的方式跳转到“控制台”屏幕；点击“设置”项后以淡入的方式跳转到“外观设置”屏幕；常驻底栏：**这一处保留现有设计，本次重构不动它**。列在草图里只为了确认它相对内容的位置与高度。
- “外观”卡片：分组卡：一张卡就是一个设置分组，卡片内是若干设置行。
- “主题模式”列表项：跟随系统 / 浅色 / 深色，点开是分段选择。
- “颜色来源”列表项：壁纸动态取色 / 自定义主色（点开是取色器）。
- “深色样式”列表项：普通黑 / AMOLED 纯黑。
- “标题与标签使用强调字重”开关：M3 Expressive 的 emphasized 字体样式。
- “表现力弹簧动效”开关：关掉则退回标准的匀速过渡。
- 导航栏：点击“主页”项后以淡入的方式跳转到“主页”屏幕；点击“服务端”项后以淡入的方式跳转到“服务端”屏幕；点击“控制台”项后以淡入的方式跳转到“控制台”屏幕；点击“设置”项后以淡入的方式跳转到“外观设置”屏幕；常驻底栏：**这一处保留现有设计，本次重构不动它**。列在草图里只为了确认它相对内容的位置与高度。
- “存储”卡片：分组卡。
- “实例目录”列表项：点开用系统目录选择器换位置，换完自动重新扫描已有服务端。
- “已安装的 Java”列表项：点开可以单独安装或卸载某个 Java 版本。
- “电池优化”列表项：没加入白名单时高亮提示，点一下跳系统设置。
- “更新通道”下拉菜单：稳定版 / 预览版。
- “关于与许可证”按钮：GPL-3.0 全文与第三方组件声明。

## 各组件的样式
以下是所用组件的参考。数值均为 M3 Expressive 的标准值，能用标准组件实现的就交给标准组件，并可根据内容适当调整。
- 导航栏：高 80dp，背景为 surfaceContainer。背景延伸到屏幕底部的手势导航区域，并按系统内边距在底部留出空间。选中项用 secondaryContainer 的胶囊指示器（宽 64dp、高 32dp）表示，图标为填充样式，标签用 labelMedium。
- 加载指示：使用 M3 Expressive 形状变化的 LoadingIndicator（旋转并在多边形之间变形）。带容器的放在 secondaryContainer 的圆形中。
- 标签片：高 32dp，圆角 8dp。选中状态用 secondaryContainer 填充并在前面显示勾选图标。横向标签片组间距 8dp，溢出时横向滚动。
- 卡片：圆角 20dp。图片区域按每张卡片的描述放在顶部、左侧、右侧或作为整卡背景（背景时从文字一侧加渐变遮罩：浅色文字用黑色，深色文字用白色）。图片保持宽高比并居中裁剪以填满区域。填充用 surfaceContainerHighest，浮起用 surfaceContainerLow 加 Level 1 阴影，描边用 1dp 的 outlineVariant 边框。标题用 titleMedium，正文用 bodyMedium。内边距 20dp，标题与正文间距 4dp，图片与文字间距 12dp。
- 按钮：中号，高 56dp，完全圆角（胶囊形）。填充用 primary，色调用 secondaryContainer，描边用 1dp 的 outline 边框。横向相连的按钮组以 3dp 间距排列，只把相邻的内侧圆角缩小到 8dp，外侧保持圆角（M3 Expressive 的 Connected button group）。
- 列表项：高 72dp，左侧图标 24dp（未指定时放在 40dp 的 primaryContainer 圆形上），主文本用 bodyLarge，辅助文本用 bodyMedium 的 onSurfaceVariant，背景为指定的颜色角色（未指定则为 surfaceContainerLow）。上下相连的列表以 3dp 间距排列，外侧圆角 28dp，相邻内侧圆角 8dp（M3 Expressive 的列表样式）。
- 文本输入框：高 56dp。描边样式圆角 16dp、边框为 outline；填充样式背景为 surfaceContainerHighest 并带下划线。聚焦时标签上浮，边框变为 2dp 的 primary。辅助文本用 bodySmall 显示在下方。
- 文本：指定的 sp 字号。标题用 onSurface，说明文字用 onSurfaceVariant，行高为字号的 1.3〜1.5 倍。点击时不加涟漪等反馈。
- 标签页：M3 的主标签页。高 48dp，标签用 titleSmall，选中项文字为 primary 并带与标签同宽的 3dp 指示条（上方圆角），下方为 outlineVariant 分割线。点击标签切换内容。
- 线性进度条：使用指定的轨道粗细（未指定则为 4dp）和圆形端帽。指定波浪形时使用 M3 Expressive 的 wavy 样式。轨道为 secondaryContainer，进度为 primary。
- 搜索栏：高 56dp，完全圆角，背景为 surfaceContainerHigh。左侧显示搜索图标，右侧显示指定图标。
- 扩展 FAB：高 56dp，圆角 16dp，左侧图标、右侧标签。
- 开关：M3 标准尺寸（轨道 52×32dp）。开为 primary，关为 surfaceContainerHighest 加 outline 边框。标签在左，开关靠右。
- 下拉菜单：外观与文本输入框相同（高 56dp，描边或填充），末尾带 arrow_drop_down 图标。按 exposed dropdown menu 实现：点击后在下方展开菜单（surfaceContainer，圆角 4dp，项高 48dp），所选值显示在输入框中。

## 整体原则
- 先根据屏幕目的判断这是什么类型的应用，并实现该类应用通常应有的功能（新建、列表、详情、编辑、删除、搜索、设置等，视情况而定），即使草图中没有画出。
- 把数据当作真实数据处理：用户创建的数据要持久化到设备（Room、DataStore 等），重启后仍保留。不要放入虚拟或示例数据，没有数据时显示空状态提示。校验输入，删除和失败要有适当的确认或提示。
- 草图没有写明的行为，根据屏幕目的和组件标签补全。未指定行为的按钮或项目要实现与其标签相符的操作（保存、发送、打开详情页等），不要什么都不做。
- 布局只需保持意图（顺序、分组、相对位置），尺寸和间距可根据内容调整。若在真机上会出问题，宁可能用也不要死守草图。
- 组件使用 Jetpack Compose material3（包含 Expressive API 的最新版） 的标准组件，库里已有的组件不要自行绘制。
- 颜色必须通过上面配色方案的角色名（primary、surfaceContainer 等）引用，不要写死颜色值。
- 屏幕边缘留 16dp，组件之间 8〜16dp，排版使用 M3 的字体样式（titleLarge、bodyMedium 等）。
- 写明“横向排成一行”的组件必须放进同一个 Row（横向容器）并在同一行显示，不要竖着堆叠或换行。行高以最高的组件为准，其余组件垂直居中。
- 写明“内部叠放”的组件要绘制在该容器（容器框或卡片）之上（以容器为背景的 Box）。这种叠放是有意为之，不要因布局原因拆开或调整顺序。前后关系按描述顺序，后写的在前面。
- 可点击的组件加涟漪和轻微缩放反馈。“返回”反向播放进入时的过渡动画，系统返回手势／返回键也要做同样的效果。
- 图标使用 Material Symbols Rounded。
- 不需要在模拟器或真机上验证。实现完成后生成已签名的 release APK 作为交付物。